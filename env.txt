# Copy and rename to .env and dev.env, fill out all fields

# Backend app
BACKEND_APP_PORT=8080
# https://www.devglan.com/online-tools/hmac-sha256-online
# expiration [ms]
BACKEND_ACCESS_TOKEN_SECRET=
BACKEND_ACCESS_TOKEN_EXPIRATION=600000
BACKEND_REFRESH_TOKEN_SECRET=
BACKEND_REFRESH_TOKEN_EXPIRATION=36000000

# Backend app OAuth2
BACKEND_OAUTH2_FRONTEND_REDIRECT_URI=https://localhost/frontend-oauth2/redirect
# dev: http://localhost:3000/frontend-oauth2/redirect

BACKEND_OAUTH2_GOOGLE_CLIENT_ID=
BACKEND_OAUTH2_GOOGLE_CLIENT_SECRET=
BACKEND_OAUTH2_GOOGLE_AUTHORIZED_REDIRECT_URI=https://localhost/oauth2/callback
# dev: http://localhost:3000/oauth2/callback

# Database
DB_HOST=
# dev: localhost
DB_PORT=
DB_NAME=
DB_USERNAME=
DB_PASSWORD=