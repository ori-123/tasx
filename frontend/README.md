# Codecool advanced project frontend

## Setup & Run

- Copy `env.txt` & rename to `.env`
- Set proxy URL to backend URL in `vite.config.js`
- Run `npm i`
- Run the app with `npm start` or `npm run dev`
