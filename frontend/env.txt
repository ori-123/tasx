# Copy and rename to .env, fill out all fields

VITE_PORT=3000
VITE_API_BASE_URL=/api/v1

# Append with /provider-name when used! Example: /oauth2/authorize/google
VITE_OAUTH2_AUTHORIZATION_URL=/oauth2/authorize
VITE_OAUTH2_REDIRECT_URL=frontend-oauth2/redirect